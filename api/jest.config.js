module.exports = {
  testEnvironment: 'node',
  clearMocks: true,
  // Não é mais necessário, pois o Jest encontra a pasta __mocks__ automaticamente
  // setupFilesAfterEnv: ['./__tests__/setup.js'],
};
